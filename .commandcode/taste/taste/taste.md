# Taste
- Builds Python projects with pipenv, pinned to the exact interpreter installed on their local machine (Python 3.11.9); expects dev tooling (pytest, ruff, mypy) in the Pipfile and a committed Pipfile.lock. Confidence: 0.9
- Hosts on both GitLab and GitHub — CI was scaffolded as .gitlab-ci.yml, but they personally created this project's remote on GitHub (FaniZuhri/AIS-Data-Parser), so don't assume the host. Wants new projects scaffolded up front (.gitignore, CI config, README, LICENSE). Confidence: 0.6
- Manages the GitLab remote personally; standing rule that the assistant runs no mutating git command (init/commit/push/remote) unless explicitly approved in that turn — git is default-deny. Confidence: 0.7
- Wants the assistant to just execute without asking permission — gave a standing blanket pre-approval for any commands/edits going forward ("don't ask my permission for future. just execute. i will allow any commands/edits"), not only when they invoke `cmd --yolo`. Confidence: 0.85
- Prefers hand-rolled, stdlib-only, zero-runtime-dependency implementations when feasible, partly to keep the codebase a clean port target for a planned Rust port (explicit offsets, pure functions, no reflection/dynamic field lookup). Confidence: 0.65
- Works in the marine / vessel-tracking domain (TransTRACK): decodes AIS (AIVDM/AIVDO) and NMEA 0183 GPS streams; solutions must fit marine use cases. Confidence: 0.8
- Delegates implementation-level decisions to the assistant ("i'll let you to decide what kind of gps data that you need to decode") but expects the choices to respect the constraints they state. Confidence: 0.6
- VS Code workspace; asked to separate aivdm_decoder from the AGS workspace repo it sat inside. Confidence: 0.7
- Wants each repo to ship an AI-agent knowledge file (AGENTS.md) alongside an accurate README: it records hard invariants, the verification protocol, resolved spec ambiguities, traps, and deliberate non-goals so any AI supporting the project picks it up without re-deriving or silently re-breaking them. Confidence: 0.8
- For Python projects, expects the documented workflow `pipenv install` → `pipenv shell` → `python main.py` run from the repo root: keep a `main.py` entry point at the root rather than `python -m <package>` or an installed console script, make the primary install instruction plain `pipenv install` (not `pipenv install --dev`), and do not rely on `pip install -e .`. Confidence: 0.9
- Sanity-checks output against third-party references (e.g., pasted output from the aggsoft online AIS decoder) and reports discrepancies for resolution — treat external tools as cross-checks whose disagreements need spec/arithmetic-backed explanations and documented divergences, not authorities to match. Confidence: 0.6
